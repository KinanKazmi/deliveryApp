import { Text, SafeAreaView } from "react-native";

export default function Orders() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Orders</Text>
    </SafeAreaView>
  );
}
