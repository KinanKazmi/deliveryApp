import { Text, SafeAreaView } from "react-native";

export default function Help() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Help</Text>
    </SafeAreaView>
  );
}
