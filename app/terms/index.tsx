import { Text, SafeAreaView } from "react-native";

export default function Terms() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Terms</Text>
    </SafeAreaView>
  );
}
