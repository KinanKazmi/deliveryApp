import { Text, SafeAreaView } from "react-native";

export default function Addresses() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Addresses</Text>
    </SafeAreaView>
  );
}
