import { Text, SafeAreaView } from "react-native";

export default function Signout() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Signout</Text>
    </SafeAreaView>
  );
}
