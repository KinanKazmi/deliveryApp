import { Text, SafeAreaView } from "react-native";

export default function Rewards() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Rewards</Text>
    </SafeAreaView>
  );
}
